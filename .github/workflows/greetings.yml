name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Hoorraaayyy !! You have just made your '' first issue'
        pr-message: 'You are doing greatt !! Just continue to contribute moree !!  '' first pr'
